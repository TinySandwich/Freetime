using UnityEngine;
using System.Collections;

public class SaveSheAs : MonoBehaviour {

	public void onClick () {
		//Open up save dialog

		//If exits correctly, update "saved" boolean to be true
	}

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}

using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class xField : MonoBehaviour {

	public void onChange() {
		/*GameObject myObj = GameObject.FindWithTag ("selected").GetComponent<GameObject>();
		InputField Xin = GameObject.FindWithTag ("XIn").GetComponent<InputField>();
		myObj.transform.position.Set(float.Parse(Xin.text), myObj.transform.position.y, myObj.transform.position.z);*/
	}
}
